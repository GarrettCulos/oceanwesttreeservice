const client = `
    type Client {
      name: String
      key: String
      secret: String
      atlassianHost: String
      email: String
      updatedAt: Date
      createdAt: Date
    }

`;
export default client;
